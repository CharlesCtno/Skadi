# Environnements virtuels (Conda, venv, etc.)
.conda
.conda_env
env/
venv/
.env
.env/
environment.yml  # (Optionnel : si tu veux le versionner, retire cette ligne)

# Fichiers Python compilés
__pycache__/
*.py[cod]
*$py.class

# Fichiers de configuration IDE/éditeur
.vscode/
.idea/
*.swp
*.swo

# Données brutes et fichiers temporaires
data/raw/
data/bike/raw/
journal/photos/
live/trips/*/photos/
*.gpx
*.jpg
*.jpeg
*.png
*.webp
*.gif
*.xlsx
*.xls

# Fichiers générés (GeoJSON, CSV traités)
#data/processed/*  # (Optionnel : si ces fichiers sont régénérés automatiquement)

# Fichiers de logs et cache
*.log
.DS_Store

# Export local de transcripts Cursor (ne pas versionner)
RECOVERED_AGENT_CHAT.txt
recovered_agent_chat.txt

# Fichiers de dépendances (optionnel : à versionner si partagé)
requirements.txt  # (À versionner si tu l'utilises)
test.temp
