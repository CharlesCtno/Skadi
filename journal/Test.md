If column T contains plain text (does not start with journal/): display the text directly in the existing activity popup, in a new paragraph below the existing stats. Widen the popup maximum width to accommodate the text — add a max-width value that can be easily adjusted (add a comment saying /* journal popup width — adjust here */)
If column T contains plain text (does not start with journal/): display the text directly in the existing activity popup, in a new paragraph below the existing stats. Widen the popup maximum width to accommodate the text — add a max-width value that can be easily adjusted (add a comment saying /* journal popup width — adjust here */)

If column T contains plain text (does not start with journal/): display the text directly in the existing activity popup, in a new paragraph below the existing stats. Widen the popup maximum width to accommodate the text — add a max-width value that can be easily adjusted (add a comment saying /* journal popup width — adjust here */)


If column T contains plain text (does not start with journal/): display the text directly in the existing activity popup, in a new paragraph below the existing stats. Widen the popup maximum width to accommodate the text — add a max-width value that can be easily adjusted (add a comment saying /* journal popup width — adjust here */)If column T contains plain text (does not start with journal/): display the text directly in the existing activity popup, in a new paragraph below the existing stats. Widen the popup maximum width to accommodate the text — add a max-width value that can be easily adjusted (add a comment saying /* journal popup width — adjust here */)If column T contains plain text (does not start with journal/): display the text directly in the existing activity popup, in a new paragraph below the existing stats. Widen the popup maximum width to accommodate the text — add a max-width value that can be easily adjusted (add a comment saying /* journal popup width — adjust here */)If column T contains plain text (does not start with journal/): display the text directly in the existing activity popup, in a new paragraph below the existing stats. Widen the popup maximum width to accommodate the text — add a max-width value that can be easily adjusted (add a comment saying /* journal popup width — adjust here */)